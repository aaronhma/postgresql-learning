# Installation

```bash
# update brew package versions
brew update

# install postgresql
brew install postgresql

# check postgres version
postgres --version

# also, you can use psql:
psql --version
```
